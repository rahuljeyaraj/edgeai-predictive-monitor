# app.yaml: The main configuration file for your Arduino App.
# This file describes the application's metadata and properties.

# The user-visible name of the application.
name: edgeai-predictive-monitor-base-station

# The icon for the application, can be an emoji or a short string.
icon: 📳

# A list of network ports that the application exposes.
# Example: [80, 443]
ports: [8080, 8081]

# A list of bricks used by this application.
# TODO(Telegram alerts, docs/DASHBOARD_IDEAS_BACKLOG.md): once App Lab is
# set up for it, add arduino:telegram_bot here (and set TELEGRAM_BOT_TOKEN
# via App Lab's UI + this app's own TELEGRAM_BOT_USERNAME env var -- the
# brick has no getMe() accessor, and the dashboard's "Connect Telegram"
# deep link (t.me/<username>?start=<token>) needs the username up front).
# Left out for now -- reverted after it broke an in-progress device build.
# Every other Telegram alerts code path already no-ops safely without it
# (main.py only wires the bot when TELEGRAM_BOT_TOKEN is set).
bricks: []
