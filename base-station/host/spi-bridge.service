[Unit]
Description=Root-owned bridge exposing /dev/spidev0.0 to the app container over a Unix socket
After=multi-user.target

[Service]
Type=simple
ExecStart=/usr/bin/python3 /usr/local/sbin/spi_bridge.py
Restart=on-failure
RestartSec=1
User=root

[Install]
WantedBy=multi-user.target
