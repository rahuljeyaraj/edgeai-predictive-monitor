# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/

# Zephyr / West build artifacts
build/
.west/

# Arduino App Lab / CLI local cache
.arduino-app-cli/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
!.vscode/extensions.json

# Claude Code personal overrides (not shared with team)
.claude/settings.local.json
.claude/CLAUDE.local.md

# Raw sensor data captures (tools/raw_capture.py output, pulled off-device
# via adb pull) -- large labeled .npz files, not source, not shared via git.
captures/
captures_*/

# offline_experiment.py --plot-out diagnostic plots (docs/RAW_CAPTURE_WORKFLOW.md
# step 7 examples write these to the repo root) -- generated, not source.
/out*.png
